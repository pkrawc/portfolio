import styled from "styled-components"
import {
  space,
  layout,
  flexbox,
  grid,
  background,
  color,
  border,
} from "styled-system"

export default styled.div`
  ${layout};
  ${space};
  ${flexbox};
  ${grid};
  ${background};
  ${border};
  ${color};
`
